class circle{
   radius=1.0;
   color=red;
   
 constructor(radius,color){
    this.radius=radius;
    this.color=color;
 }
 getarea(){
   return this.radius*','*this.color;
   
 }
 setradius(radius){
  return this.radius;
 }
setcolor(color){
  return this.color;

}
prototype={
  area: function area(){
    return Math.pow(this.radius, 1.0) * math.radius;
  }
  
}
color1={
  color: function col(){
    return (this.color);
  }
}
};
console.log(prototype,color1);
